# 编译产物（progress-server 二进制）
progress-server
*.exe
*.test
*.test.bin

# Vendor 目录（本项目走 go module，不 vendor）
vendor/

# 本地环境配置（含 DB_URL/JWT_SECRET 等敏感信息）
.env
.env.*
# 但 .env.example 是占位文档，必须入库供新人参考
!.env.example

# 本地文件存储与覆盖率
data/
coverage.out
coverage.html

# IDE 临时文件
.idea/
*.swp
